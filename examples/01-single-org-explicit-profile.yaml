# Demonstrates:
# - Explicit AWS profile usage
# - Explicit single-region configuration
#
# Builds on 01 by adding a profile and explicit regions.

schema_version: 1

organizations:
  - name: profile-org

    regions:
      - us-east-1
