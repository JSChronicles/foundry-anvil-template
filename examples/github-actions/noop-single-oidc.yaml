schema_version: 1
max_parallel_targets: 1

organizations:
  - name: single-oidc-org
    regions:
      - us-east-1
    role_name: OrganizationAccountAccessRole
    max_workers: 10
    fail_fast: false
    dry_run: true
    tasks:
      - name: noop
