{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://anvil.local/schemas/orgs.schema.v1.json",
  "title": "Anvil Organization Configuration",
  "description": "Declarative configuration for executing tasks across AWS Organizations.",
  "type": "object",
  "required": [
    "schema_version",
    "organizations"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Configuration schema version."
    },
    "max_parallel_targets": {
      "$ref": "common.schema.v1.json#/$defs/max_parallel_targets"
    },
    "organizations": {
      "type": "array",
      "minItems": 1,
      "description": "List of AWS Organizations to process.",
      "items": {
        "type": "object",
        "required": [
          "name"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/name"
          },
          "profile": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/profile"
          },
          "regions": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/regions",
            "description": "Organization regions for task execution. Use explicit regions, 'all' by itself, glob selectors, or mixed glob and explicit selectors.",
            "examples": [
              [
                "us-east-1"
              ],
              [
                "all"
              ],
              [
                "us-*"
              ],
              [
                "us-*",
                "eu-*"
              ],
              [
                "us-*",
                "ca-central-1"
              ]
            ]
          },
          "role_name": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/role_name"
          },
          "assume_role_in_management": {
            "type": "boolean",
            "default": false,
            "description": "If true, the management/payer account uses the same configured role-assumption path as member accounts. This supports delegated-administrator broker workflows."
          },
          "tasks": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/tasks"
          },
          "max_workers": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/max_workers"
          },
          "max_parallel_regions": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/max_parallel_regions"
          },
          "fail_fast": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/fail_fast"
          },
          "dry_run": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/dry_run"
          },
          "metadata": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/metadata"
          },
          "post_run": {
            "$ref": "common.schema.v1.json#/$defs/baseTargetProperties/post_run"
          },
          "include": {
            "allOf": [
              {
                "$ref": "common.schema.v1.json#/$defs/accountIdList"
              }
            ],
            "description": "Explicit list of AWS account IDs to include."
          },
          "exclude": {
            "allOf": [
              {
                "$ref": "common.schema.v1.json#/$defs/accountIdList"
              }
            ],
            "description": "List of AWS account IDs to exclude."
          }
        },
        "not": {
          "required": [
            "include",
            "exclude"
          ]
        }
      }
    }
  }
}
