schema_version: 2

targets:
  - name: gcp-extra-smoke
    provider:
      name: gcp
      mode: projects
      options: {}
    regions:
      - us-central1
    dry_run: true
    tasks:
      - name: setup_anvil_smoke

    include:
      - setup-anvil-smoke-project
